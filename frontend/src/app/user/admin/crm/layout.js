export const metadata = {
    title: 'CRM',
    description: 'Filter and export leads, review analytics, scoring, and lead history.',
};

export default function CrmLayout({ children }) {
    return children;
}
