export const metadata = {
  title: "Auto-Followup Sequence Manager",
  description: "Design and schedule automated follow-up sequences for your leads to ensure no prospective client is left waiting without a response.",
};

export default function FollowUpsLayout({ children }) {
  return children;
}
