export const metadata = {
  title: "Workspace Business Dashboard",
  description: "Monitor your workspace metrics, track conversational AI performance, manage recent activities, and view quick insights to optimize client engagement.",
};

export default function DashboardLayout({ children }) {
  return children;
}
