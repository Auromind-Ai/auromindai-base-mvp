export const metadata = {
  title: "Credits & Wallet Balance",
  description: "View your workspace credit balance, track usage statistics, top up your wallet, and manage resource allocations for AI and message delivery.",
};

export default function CreditsLayout({ children }) {
  return children;
}
