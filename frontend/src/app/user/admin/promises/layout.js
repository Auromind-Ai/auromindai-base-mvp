export const metadata = {
  title: "Founder Assistant & Commitments",
  description: "Track client-facing promises, schedule commitment deadlines, review overdue tasks, and use the AI assistant to keep your business word.",
};

export default function PromisesLayout({ children }) {
  return children;
}
