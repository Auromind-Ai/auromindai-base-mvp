export const metadata = {
  title: "Calendar & Appointment Scheduling",
  description: "Track client appointments, schedule meetings, sync external calendars, and manage bookings handled by your automated AI conversation agents.",
};

export default function CalendarLayout({ children }) {
  return children;
}
