export const metadata = {
  title: "Workspace Settings & Preferences",
  description: "Configure account details, set notification preferences, manage security and two-factor authentication, and adjust regional workspace values.",
};

export default function SettingsLayout({ children }) {
  return children;
}
