export const metadata = {
  title: "Create New Message Template",
  description: "Design a new messaging template, configure custom placeholders, and draft standardized replies for your team and automated AI agents.",
};

export default function CreateTemplateLayout({ children }) {
  return children;
}
