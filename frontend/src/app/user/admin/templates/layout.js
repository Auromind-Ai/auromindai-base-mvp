export const metadata = {
  title: "Response & Message Templates",
  description: "Create and store pre-approved response templates, automated messaging scripts, and quick-reply answers to maintain consistent client communication.",
};

export default function TemplatesLayout({ children }) {
  return children;
}
