export const metadata = {
  title: "Knowledge Base & AI Brain",
  description: "Upload documents, sync web resources, and build your custom AI knowledge base to train agents and power accurate RAG responses for clients.",
};

export default function BrainLayout({ children }) {
  return children;
}
