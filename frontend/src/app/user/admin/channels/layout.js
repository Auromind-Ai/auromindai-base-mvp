export const metadata = {
  title: "Messaging Channel Integrations",
  description: "Connect and manage your communication channels including WhatsApp, Instagram, and Web Chat to centralize customer conversations.",
};

export default function ChannelsLayout({ children }) {
  return children;
}
