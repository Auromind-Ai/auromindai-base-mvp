export const metadata = {
  title: "Gmail & Email Inbox Integration",
  description: "Connect your business email accounts, view inbox messages, review AI-generated draft responses, and send replies directly from the platform.",
};

export default function EmailLayout({ children }) {
  return children;
}
