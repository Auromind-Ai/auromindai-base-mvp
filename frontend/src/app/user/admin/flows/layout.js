export const metadata = {
  title: "Interactive Message Flows",
  description: "Build and edit step-by-step chat sequences, design interactive message flows, and deploy automated logic for client-facing channels.",
};

export default function FlowsLayout({ children }) {
  return children;
}
