export const metadata = {
  title: "Automations & Workflow Triggers",
  description: "Create automated response triggers, configure webhook notifications, and design smart messaging rules to optimize customer communications.",
};

export default function AutomationLayout({ children }) {
  return children;
}
