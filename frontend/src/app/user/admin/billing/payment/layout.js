export const metadata = {
  title: "Billing Payment & Checkout",
  description: "Securely process payments, confirm subscription changes, and add funds to your workspace wallet to keep your AI agents running smoothly.",
};

export default function BillingPaymentLayout({ children }) {
  return children;
}
