export const metadata = {
  title: "AI Governance & Guardrails",
  description: "Manage Model Context Protocol rules, define safety guardrails, audit AI decision logs, and authorize human override actions for governance.",
};

export default function AiControlLayout({ children }) {
  return children;
}
