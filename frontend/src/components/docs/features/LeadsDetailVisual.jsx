'use client';

import DocumentationScreenshot from '@/components/docs/DocumentationScreenshot';

export default function LeadsDetailVisual() {
  return (
    <DocumentationScreenshot
      src="/images/doc-images/leads-crm-overview.png"
      alt="Leads and CRM with a lead list, selected conversation, score, favorites, and Lead Overview"
      caption="Illustrative screenshot with sample lead data."
      aspectRatio="aspect-[1670/941] [&_img]:object-contain"
    />
  );
}
